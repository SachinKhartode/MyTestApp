import { AccessElementDirective } from './access-element.directive';

describe('AccessElementDirective', () => {
  it('should create an instance', () => {
    const directive = new AccessElementDirective();
    expect(directive).toBeTruthy();
  });
});
