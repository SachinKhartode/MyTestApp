import { CammelCaseDirective } from './cammel-case.directive';

describe('CammelCaseDirective', () => {
  it('should create an instance', () => {
    const directive = new CammelCaseDirective();
    expect(directive).toBeTruthy();
  });
});
